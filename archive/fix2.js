const fs = require('fs'); let code = fs.readFileSync('app.js', 'utf8'); code = code.replace(/document\.getElementById\('orderSenderName'\)\.addEventListener/g, 'document.getElementById(orderSenderName)?.addEventListener'); code = code.replace(/document\.getElementById\('orderSameAsSender'\)\.addEventListener/g, 'document.getElementById(orderSameAsSender)?.addEventListener'); code = code.replace(/document\.getElementById\('orderDeliveryType'\)\.addEventListener/g, 'document.getElementById(orderDeliveryType)?.addEventListener'); code = code.replace(/document\.getElementById\('orderArrivalDate'\)\.addEventListener/g, 'document.getElementById(orderArrivalDate)?.addEventListener'); code = code.replace(/document\.getElementById\('initSystemBtn'\)\.onclick/g, 'let initBtn = document.getElementById(initSystemBtn); if(initBtn) initBtn.onclick'); fs.writeFileSync('app.js', code);
